# There should be no dependency in production that isn't in development, so rigorously review anything before putting
# it in here.

-r base.txt

MySQL-python==1.2.5		# GPL License
PyYAML==3.12			# MIT License
gunicorn==0.17.4		# MIT
python-memcached==1.58
nodeenv==1.1.2	# BSD
